# Test Images

Drop your construction site images here (JPG or PNG).

Examples: unfinished walls, pipes, beams, ducts.

Then run:
```bash
cd backend
python test_vlm_pipeline.py --frames ../test_images --zone floor_3
```
